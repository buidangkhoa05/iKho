# Bazel configuration for monorepo

# Enable Bzlmod (default in Bazel 7+)
common --enable_bzlmod

# Build optimizations
build --jobs=auto
build --verbose_failures

# Test configuration
test --test_output=errors

# Platform-specific settings
build:windows --enable_runfiles

# Remote caching (optional - uncomment to enable)
# build --remote_cache=grpc://localhost:9092
