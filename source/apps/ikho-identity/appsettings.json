{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Database": {
    "ConnectionString": "Host=localhost;Port=5432;Database=ikho_identity;Username=ikho;Password=ikho"
  },
  "MessageBroker": {
    "BootstrapServers": "localhost:9092",
    "ClientId": "ikho-identity",
    "TopicPrefix": "identity"
  },
  "Jwt": {
    "// NOTE": "Checked-in defaults. Real values come from Jwt__Authority/Jwt__Audience env vars (see source/.env.example) for docker-compose, or dotnet user-secrets for local `nx serve`/`dotnet run`.",
    "Authority": "",
    "Audience": ""
  },
  "Clerk": {
    "// NOTE": "Checked-in defaults. Real values come from Clerk__SecretKey/Clerk__WebhookSigningSecret env vars (see source/.env.example) for docker-compose, or dotnet user-secrets for local `nx serve`/`dotnet run`.",
    "SecretKey": "",
    "WebhookSigningSecret": ""
  },
  "Services": {
    "Organization": {
      "BaseUrl": "http://localhost:5151"
    }
  }
}
