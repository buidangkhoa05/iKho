<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- The test project lives in a subfolder of this project directory; exclude it from this
         project's default globs so its files aren't compiled twice. -->
    <Compile Remove="Ikho.Identity.Tests/**/*.cs" />
    <Content Remove="Ikho.Identity.Tests/**/*" />
    <None Remove="Ikho.Identity.Tests/**/*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\libs\ikho-shared-library\Ikho.SharedLibrary.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Lets the test project call internal members (e.g. ClerkIdentityProvider.IsValidSignature)
         directly for known-answer-vector tests without widening the public API surface. -->
    <InternalsVisibleTo Include="Ikho.Identity.Tests" />
  </ItemGroup>

</Project>
