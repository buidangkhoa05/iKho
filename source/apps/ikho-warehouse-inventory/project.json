{
  "name": "Ikho.Warehouse.Inventory",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "docker build -f docker/dotnet.Dockerfile --build-arg PROJECT_PATH=apps/ikho-warehouse-inventory/Ikho.Warehouse.Inventory.csproj --build-arg ASSEMBLY_NAME=Ikho.Warehouse.Inventory -t ikho/warehouse-inventory:local ."
      },
      "metadata": {
        "description": "Build the Docker image for this service",
        "technologies": ["docker"]
      }
    }
  }
}
